# Copy to .env in the repo root (never commit .env).
# Supabase: Project Settings → API

SUPABASE_URL=https://your-project-ref.supabase.co
# Service role key (secret); do not use the anon key for these spiders.
SUPABASE_KEY=
